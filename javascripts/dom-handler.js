"use strict";

/*
var submit = document.getElementById("button");
var field = document.getElementById("input").value;
var zipcode = new RegExp(/[0-9][^a-z]{5}/).field;

submit.addEventListener("click", getUserput);

function getUserput(event) {
    if (event.keyCode === 13 && ){
        alert("zipcode is valid");
    } else  {
        alert("zipcode is invalid");
    } 
};


